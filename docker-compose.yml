php:
  build: ./docker/php
  volumes:
    - ./:/var/www/

node:
  build: ./docker/node
  volumes:
    - ./:/var/www/

nginx:
  build: ./docker/nginx
  ports:
    - "443:443"
  volumes:
    - ./:/var/www/
  links:
    - php