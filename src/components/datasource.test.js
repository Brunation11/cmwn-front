import React from 'react'; //eslint-disable-line no-unused-vars
import { expect } from 'chai';
import { mount } from 'enzyme';
//import { connect } from 'react-redux';

import GenerateDataSource from 'components/datasource';

var checkDatasourceGeneration = function (data, currentUser) {
    //var { {DataSource: Component}, mapStateToProps} = GenerateDataSource('test', 'datasource', {doNotMapStateToProps: true});
    //const WRAPPER = mount(profile);
    //expect(WRAPPER.instance()).to.be.instanceOf(Profile);
};
